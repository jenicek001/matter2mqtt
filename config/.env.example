# Environment Configuration for Matter Stack
# Copy this to .env and customize for your setup
# IMPORTANT: .env is in .gitignore - NEVER commit credentials to the repository!

# MQTT Configuration
# Set these if your Mosquitto broker requires authentication
MQTT_USERNAME=
MQTT_PASSWORD=

# If using different MQTT broker
# MQTT_BROKER=localhost
# MQTT_PORT=1883

# MQTT Base Topic (default: matter)
# MQTT_BASE_TOPIC=matter

# Matter Server URL (default: ws://localhost:5580/ws)
# MATTER_SERVER_URL=ws://localhost:5580/ws

# Timezone for bridge logging (default: UTC)
# All MQTT timestamps use UTC+00:00 regardless of this setting
# Set to your local timezone for more readable logs
# Examples: Europe/Prague, America/New_York, Asia/Tokyo
TZ=UTC

# Device-specific configuration
# You can set friendly names for your devices here
DEVICE_1_NAME=Living Room Temperature Sensor
DEVICE_2_NAME=Living Room Air Quality Sensor

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Notes:
# - Leave MQTT_USERNAME and MQTT_PASSWORD empty if using anonymous access
# - Never commit real credentials to the repository
# - The docker-compose.yml will automatically use these values from .env
# - Restart containers after changing: docker-compose restart
# - All MQTT message timestamps are ISO 8601 UTC format (+00:00)
